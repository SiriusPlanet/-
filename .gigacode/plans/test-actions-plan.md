# 🧪 План тестирования страницы акций (actions.html)

## 📅 Дата: 2026-06-14
## 🎯 Цель: Проверить, что карточки товаров и новостей — это одно и то же (разница только в дизайне)

---

## 🧠 Понимание архитектуры

### 1. Карточки товаров и новостей — одно и то же
- **Логика одинаковая**: JSON-структура, рендеринг, сетка
- **Разница только в дизайне**:
  - Новости: `news-card` (стили в `style.css`)
  - Товары/Акции: `product-card` (стили в `style.css`)

### 2. Данные уже есть
- `data/news/news-1.json` — акция с `discount: 25`, `category: "Акции"`
- `data/news/news-2.json` — новинка с `discount: 10`, `category: "Техника"`

### 3. Структура данных
```json
{
  "id": "news-1",
  "title": "Супер скидка 25% на всю технику!",
  "date": "2026-06-14",
  "preview": "...",
  "content": "...",
  "image": "tech-sale.jpg",
  "discount": 25,        // ← поле скидки
  "category": "Акции"    // ← поле категории
}
```

---

## 🧪 Тест-кейсы

### TC-1: Пустая страница акций
**Цель:** Проверить, что `actions.html` сейчас пустая (без карточек)

**Действия:**
1. Открыть `actions.html` в браузере
2. Проверить содержимое секции `<section class="catalog">`

**Ожидаемый результат:**
- Секция с заголовком "Квантовые акции"
- `<div class="products-grid"></div>` — пустой контейнер
- Карточки отсутствуют

**Фактический результат (сейчас):**
- ✅ Секция есть
- ✅ Контейнер есть
- ❌ Карточки добавлены вручную в HTML (не через JS)

**Вывод:** Карточки добавлены статически, нужно сделать динамическую загрузку.

---

### TC-2: Динамическая загрузка карточек акций
**Цель:** Проверить, что карточки акций загружаются из `data/news/*.json`

**Действия:**
1. Подключить `NewsPageConstructor` к `actions.html`
2. Вызвать `loadNews()` и отфильтровать по `category === 'Акции'`
3. Проверить, что отображаются только акции

**Ожидаемый результат:**
- Загружается `news-1.json` (скидка 25%)
- Загружается `news-2.json` (скидка 10%) — если `category === 'Акции'`
- Карточки отображаются в сетке

**Фактический результат (сейчас):**
- ❌ Динамическая загрузка не реализована
- ✅ Статические карточки есть в HTML (но это не то же самое)

---

### TC-3: Отображение бейджа скидки
**Цель:** Проверить, что на карточках акций есть бейдж "скидка X%"

**Действия:**
1. Открыть `actions.html`
2. Посмотреть на карточки
3. Проверить, есть ли угол скидки

**Ожидаемый результат:**
- На карточке акции есть бейдж `-%` в правом верхнем углу
- Цвет: красный градиент (как в `news-card-constructor.js`)
- Позиция: `position: absolute; top: 10px; right: 10px; transform: rotate(10deg)`

**Фактический результат (сейчас):**
- ✅ Бейджи есть в статических карточках (HTML)
- ❌ Нет JS-логики для добавления бейджа

---

### TC-4: Фильтрация по категории
**Цель:** Проверить, что на странице акций только акции, а в каталоге — только товары

**Действия:**
1. Открыть `actions.html` → проверить, что только `category === 'Акции'`
2. Открыть `catalog.html` → проверить, что только `category === 'Товары'` (или `category === undefined`)
3. Открыть `news.html` → проверить, что все новости (все категории)

**Ожидаемый результат:**
- `actions.html`: только акции
- `catalog.html`: только товары
- `news.html`: все новости

**Фактический результат (сейчас):**
- ❌ Нет фильтрации (все карточки статические)
- ❌ Категории не используются

---

### TC-5: Адаптивная сетка
**Цель:** Проверить, что карточки акций адаптивно раскладываются по сетке

**Действия:**
1. Открыть `actions.html` на десктопе (1920px)
2. Проверить, что карточки в 3 колонки
3. Уменьшить окно до 768px → проверить 2 колонки
4. Уменьшить до 480px → проверить 1 колонку

**Ожидаемый результат:**
- `grid-template-columns: repeat(auto-fit, minmax(18rem, 1fr))`
- Адаптивность работает

**Фактический результат (сейчас):**
- ✅ Сетка есть (CSS в `style.css`)
- ❌ Карточки статические (не проверить JS-логику)

---

## 🔍 Анализ текущего состояния

### В `actions.html` сейчас:
```html
<!-- Статические карточки -->
<div class="products-grid">
    <div class="product-card">...</div> <!-- Акция 1 -->
    <div class="product-card">...</div> <!-- Акция 2 -->
    ...
</div>
```

### Что нужно сделать:
1. **Удалить статические карточки** из `actions.html`
2. **Добавить JS-логику** (как в `news.html`):
   - Подключить `NewsPageConstructor` или создать новый модуль `ActionsPageConstructor`
   - Фильтровать по `category === 'Акции'`
   - Рендерить карточки в `products-grid`

### Варианты реализации:

#### Вариант A: Использовать `NewsPageConstructor` (быстрое)
```javascript
// В actions.html добавить:
<script type="module" src="static/js/news-page-constructor.js"></script>
<script>
document.addEventListener('DOMContentLoaded', () => {
    const constructor = new NewsPageConstructor({
        containerSelector: '.products-grid',
        categoryFilter: 'Акции' // ← добавить фильтрацию
    });
    constructor.init();
});
</script>
```

#### Вариант B: Создать отдельный `ActionsPageConstructor` (чистое)
```javascript
// static/js/actions-page-constructor.js
import { NewsPageConstructor } from './news-page-constructor.js';

export class ActionsPageConstructor extends NewsPageConstructor {
    constructor() {
        super({
            containerSelector: '.products-grid',
            tickerContainerSelector: null // на странице акций нет бегущей строки
        });
    }

    async loadNews() {
        await super.loadNews();
        // Фильтруем только акции
        this.newsList = this.newsList.filter(n => n.category === 'Акции' || n.discount);
    }

    createCardElement(news, index) {
        const card = super.createCardElement(news, index);
        // Можно добавить специфичные стили для акций
        return card;
    }
}
```

---

## 🎯 Выводы

### Что уже работает:
- ✅ Структура `actions.html` есть
- ✅ Сетка `products-grid` есть
- ✅ Стили `product-card` есть
- ✅ Статические карточки есть (но это не динамически)

### Что нужно добавить:
- ❌ Динамическая загрузка карточек из `data/news/*.json`
- ❌ Фильтрация по `category === 'Акции'`
- ❌ JS-логика для добавления бейджа скидки (если не использовать `NewsCardConstructor`)
- ❌ Удаление статических карточек из HTML

### Что НЕ нужно делать:
- ✅ НЕ менять JSON-структуру (уже есть `discount`, `category`)
- ✅ НЕ менять сервер (`siss.py` уже работает с этими полями)
- ✅ НЕ менять `NewsCardConstructor` (он уже поддерживает `discount`)

---

## 🚀 Следующие шаги

1. **Выбрать вариант реализации** (A или B)
2. **Обновить `actions.html`**:
   - Удалить статические карточки
   - Добавить JS-скрипт для динамической загрузки
3. **Протестировать**:
   - Открыть `actions.html` → проверить карточки
   - Удалить `news-1.json` → проверить, что карточка исчезла
   - Добавить новую акцию через форму → проверить, что она появилась
4. **Доработать** (если нужно):
   - Улучшить дизайн карточек акций
   - Добавить анимации
   - Добавить фильтрацию по дате

---

**Статус:** Готов к реализации  \n**Автор:** GigaCode  \n**Версия:** 1.0
